# Money decisions

One line per purchase: date + amount + outcome. Referenced by PLAN.md conventions.

## Pending (blocked on Malte)

- HIBP API paid tier — $3.95/mo. Unblocks: rate-limit-free breach checks
  (`PLAN.md` money plan says buy). Wiring lands with the HIBP paid-tier work item.
- PimEyes one-time deep scan — ~€30, once at baseline. Unblocks: face-search
  gap; results imported manually (import flow is the same work item).

## Decided

- 2026-07: SearXNG sidecar — €0 (self-hosted Quadlet). Replaced the planned
  Brave Search API top-ups as the primary DDG alternative; keep Brave API as
  ad-hoc fallback only if SearXNG proves insufficient.
